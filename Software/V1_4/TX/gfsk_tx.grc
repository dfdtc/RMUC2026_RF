options:
  parameters:
    author: dfdtc
    catch_exceptions: 'True'
    category: '[GRC Hier Blocks]'
    cmake_opt: ''
    comment: ''
    copyright: dfdtc
    description: ''
    gen_cmake: 'On'
    gen_linking: dynamic
    generate_options: qt_gui
    hier_block_src_path: '.:'
    id: gfsk_tx
    max_nouts: '0'
    output_language: python
    placement: (0,0)
    qt_qss_theme: ''
    realtime_scheduling: ''
    run: 'True'
    run_command: '{python} -u {filename}'
    run_options: prompt
    sizing_mode: fixed
    thread_safe_setters: ''
    title: gfsk_tx
    window_size: (1000,1000)
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [8, 8]
    rotation: 0
    state: enabled

blocks:
- name: D
  id: variable
  parameters:
    comment: ''
    value: bw/2-samp_rate/sps
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [280, 8.0]
    rotation: 0
    state: enabled
- name: access_code
  id: variable
  parameters:
    comment: ''
    value: '"0001011011101000110100110111011100010101000111000111000100101101"'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [392, 112.0]
    rotation: 0
    state: enabled
- name: bw
  id: variable
  parameters:
    comment: ''
    value: 0.820e6
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [576, 112.0]
    rotation: 0
    state: enabled
- name: cent_freq
  id: variable
  parameters:
    comment: ''
    value: 433.2e6
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [664, 112.0]
    rotation: 0
    state: enabled
- name: rmuc_boardcast_format
  id: variable_header_format_default
  parameters:
    access_code: '"0010111101101111010011000111010010111001000101000100100100101110"'
    bps: '1'
    comment: ''
    threshold: '4'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [8, 240.0]
    rotation: 0
    state: enabled
- name: rmuc_inter_format
  id: variable_header_format_default
  parameters:
    access_code: '"0001011011101000110100110111011100010101000111000111000100101101"'
    bps: '1'
    comment: ''
    threshold: '4'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [8, 136.0]
    rotation: 0
    state: enabled
- name: samp_rate
  id: variable
  parameters:
    comment: ''
    value: 1e6
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [184, 8.0]
    rotation: 0
    state: enabled
- name: sps
  id: variable
  parameters:
    comment: ''
    value: '52'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [184, 80.0]
    rotation: 0
    state: enabled
- name: symb_rate
  id: variable
  parameters:
    comment: ''
    value: samp_rate/sps
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [264, 80.0]
    rotation: 0
    state: enabled
- name: blocks_msgpair_to_var_0
  id: blocks_msgpair_to_var
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    target: '"cent_freq"'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [856, 136.0]
    rotation: 0
    state: enabled
- name: blocks_msgpair_to_var_1
  id: blocks_msgpair_to_var
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    target: '"bw"'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [856, 72.0]
    rotation: 0
    state: enabled
- name: blocks_msgpair_to_var_1_0
  id: blocks_msgpair_to_var
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    target: '"access_code"'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [856, 8.0]
    rotation: 0
    state: enabled
- name: blocks_tagged_stream_mux_0
  id: blocks_tagged_stream_mux
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    lengthtagname: '"packet_len"'
    maxoutbuf: '0'
    minoutbuf: '0'
    ninputs: '2'
    tag_preserve_head_pos: '0'
    type: byte
    vlen: '1'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [448, 464.0]
    rotation: 0
    state: enabled
- name: digital_gfsk_mod_0
  id: digital_gfsk_mod
  parameters:
    affinity: ''
    alias: ''
    bt: '0.35'
    comment: ''
    do_unpack: 'True'
    log: 'False'
    maxoutbuf: '0'
    minoutbuf: '0'
    samples_per_symbol: sps
    sensitivity: 2*3.14*D/samp_rate
    verbose: 'False'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [688, 456.0]
    rotation: 0
    state: enabled
- name: digital_protocol_formatter_bb_0
  id: digital_protocol_formatter_bb
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    format: rmuc_inter_format
    len_tag_key: '"packet_len"'
    maxoutbuf: '0'
    minoutbuf: '0'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [208, 368.0]
    rotation: 0
    state: enabled
- name: epy_block_0
  id: epy_block
  parameters:
    _source_code: "# This work is based on an unlicensed open source project from:\
      \ SCNU-PIONEER\n# From repositorie: https://github.com/SCNU-PIONEER/PinyRadio\n\
      # All copyright and credit belongs to the original author\n# The code is used\
      \ here for educational and research purposes only\n# Keep this notice when you\
      \ use or modify the code\nimport numpy as np\nfrom gnuradio import gr\nimport\
      \ crc\nimport time\nimport random\n\n\n# --- Official CRC8 (poly=0x31) init\
      \ & table ---\nrm_crc8 = crc.Configuration(8,0x31,0xff,0,1,1)\nrm_crc16 = crc.Configuration(16,0x1021,0xffff,0,1,1)\n\
      header_crc_calc = crc.Calculator(rm_crc8,True)\nframe_crc_calc = crc.Calculator(rm_crc16,True)\n\
      \n\n# --- Official CRC16 table & init (little-endian output) ---\nCRC16_INIT\
      \ = 0xFFFF\n\ndef _crc8(data):\n\n    return header_crc_calc.checksum(data)\
      \ & 0xFF\n\n\ndef _crc16(data):\n\n    return frame_crc_calc.checksum(data)\
      \ & 0xFFFF\n\n\nCMD_OPTIONS = {\n    # name: (cmd_id, data_length)\n    \"enemy_pos\"\
      : (0x0A01, 24),\n    \"enemy_hp\": (0x0A02, 12),\n    \"enemy_ammo\": (0x0A03,\
      \ 10),\n    \"buff_state\": (0x0A04, 8),\n    \"gains\": (0x0A05, 36),\n   \
      \ \"jamming\": (0x0A06, 6),\n}\n\n\n\n# --- \u534F\u8BAE\u5B57\u6BB5\u7ED3\u6784\
      \u5B9A\u4E49\uFF08\u4E0E radar.yaml \u4FDD\u6301\u540C\u6B65\uFF09 ---\n_PROTO_FIELDS\
      \ = {\n    \"enemy_pos\": [\n        (\"hero_x\", 2, \"uint16\"), (\"hero_y\"\
      , 2, \"uint16\"),\n        (\"engineer_x\", 2, \"uint16\"), (\"engineer_y\"\
      , 2, \"uint16\"),\n        (\"infantry_3_x\", 2, \"uint16\"), (\"infantry_3_y\"\
      , 2, \"uint16\"),\n        (\"infantry_4_x\", 2, \"uint16\"), (\"infantry_4_y\"\
      , 2, \"uint16\"),\n        (\"aerial_x\", 2, \"uint16\"), (\"aerial_y\", 2,\
      \ \"uint16\"),\n        (\"sentry_x\", 2, \"uint16\"), (\"sentry_y\", 2, \"\
      uint16\"),\n    ],\n    \"enemy_hp\": [\n        (\"hero_hp\", 2, \"uint16\"\
      ), (\"engineer_hp\", 2, \"uint16\"),\n        (\"infantry_3_hp\", 2, \"uint16\"\
      ), (\"infantry_4_hp\", 2, \"uint16\"),\n        (\"reserved\", 2, \"uint16\"\
      ), (\"sentry_hp\", 2, \"uint16\"),\n    ],\n    \"enemy_ammo\": [\n        (\"\
      hero_ammo\", 2, \"uint16\"), (\"infantry_3_ammo\", 2, \"uint16\"),\n       \
      \ (\"infantry_4_ammo\", 2, \"uint16\"), (\"aerial_ammo\", 2, \"uint16\"),\n\
      \        (\"sentry_ammo\", 2, \"uint16\"),\n    ],\n    \"buff_state\": [\n\
      \        (\"remaining_gold\", 2, \"uint16\"), (\"total_gold\", 2, \"uint16\"\
      ),\n        (\"macro_bits\", 4, \"uint32\"),  # 4\u5B57\u8282\u4F4D\u57DF\uFF0C\
      \u5EFA\u8BAE\u7528int\u4F20\u5165\n    ],\n    \"gains\": [\n        (\"hero_health_regen\"\
      , 1, \"uint8\"), (\"hero_cooling_boost\", 2, \"uint16\"),\n        (\"hero_defense_boost\"\
      , 1, \"uint8\"), (\"hero_defense_debuff\", 1, \"uint8\"),\n        (\"hero_attack_boost\"\
      , 2, \"uint16\"), (\"engineer_health_regen\", 1, \"uint8\"),\n        (\"engineer_cooling_boost\"\
      , 2, \"uint16\"), (\"engineer_defense_boost\", 1, \"uint8\"),\n        (\"engineer_defense_debuff\"\
      , 1, \"uint8\"), (\"engineer_attack_boost\", 2, \"uint16\"),\n        (\"infantry_3_health_regen\"\
      , 1, \"uint8\"), (\"infantry_3_cooling_boost\", 2, \"uint16\"),\n        (\"\
      infantry_3_defense_boost\", 1, \"uint8\"), (\"infantry_3_defense_debuff\", 1,\
      \ \"uint8\"),\n        (\"infantry_3_attack_boost\", 2, \"uint16\"), (\"infantry_4_health_regen\"\
      , 1, \"uint8\"),\n        (\"infantry_4_cooling_boost\", 2, \"uint16\"), (\"\
      infantry_4_defense_boost\", 1, \"uint8\"),\n        (\"infantry_4_defense_debuff\"\
      , 1, \"uint8\"), (\"infantry_4_attack_boost\", 2, \"uint16\"),\n        (\"\
      sentry_health_regen\", 1, \"uint8\"), (\"sentry_cooling_boost\", 2, \"uint16\"\
      ),\n        (\"sentry_defense_boost\", 1, \"uint8\"), (\"sentry_defense_debuff\"\
      , 1, \"uint8\"),\n        (\"sentry_attack_boost\", 2, \"uint16\"), (\"sentry_posture\"\
      , 1, \"uint8\"),\n    ],\n    \"jamming\": [\n        (\"key\", 6, \"ascii6\"\
      ),\n    ],\n}\n\ndef _encode_payload(cmd_option, payload, pad_mode=\"pad\"):\n\
      \    fields = _PROTO_FIELDS.get(cmd_option)\n    if not fields:\n        # fallback:\
      \ treat as ascii string\n        if isinstance(payload, str):\n            return\
      \ [ord(c) for c in payload]\n        else:\n            raise ValueError(f\"\
      No field def for {cmd_option}, and payload not str\")\n    out = bytearray()\n\
      \    if not isinstance(payload, dict):\n        raise ValueError(f\"For cmd_option\
      \ {cmd_option}, payload must be dict\")\n    for name, size, typ in fields:\n\
      \        v = payload.get(name, 0)\n        if typ == \"uint16\":\n         \
      \   out += int(v).to_bytes(2, \"little\", signed=False)\n        elif typ ==\
      \ \"uint8\":\n            out += int(v).to_bytes(1, \"little\", signed=False)\n\
      \        elif typ == \"uint32\":\n            out += int(v).to_bytes(4, \"little\"\
      , signed=False)\n        elif typ == \"ascii6\":\n            s = str(v)[:6].ljust(6,\
      \ '\\0')\n            out += bytes([ord(c) for c in s])\n        else:\n   \
      \         raise ValueError(f\"Unknown type {typ}\")\n    return list(out)\n\n\
      \nimport threading\nimport zmq\n\nclass byte_vector_source(gr.sync_block):\n\
      \    \"\"\"\n    \u5C06\u8F93\u5165\u6570\u636E\uFF08dict\u6216\u5B57\u7B26\u4E32\
      \uFF09\u5C01\u88C5\u4E3A RoboMaster \u534F\u8BAE\u5E27\u5E76\u5FAA\u73AF\u8F93\
      \u51FA\u3002\n    \u652F\u6301\u672C\u5730\u53C2\u6570\uFF08mode=local\uFF09\
      \u6216\u5916\u90E8ZMQ\u5BA2\u6237\u7AEF\u53C2\u6570\uFF08mode=client\uFF09\u3002\
      \n    \u5E27\u7ED3\u6784: [SOF|len(2)|seq|CRC8][cmd_id(2)][data][CRC16(2)]\n\
      \    \"\"\"\n\n    def __init__(self, sample_rate = 1e6, payload=None, cmd_option=\"\
      enemy_pos\", seq=-1,chunk_bits = 1, pad_mode=\"pad\", mode=\"local\", rand_pad\
      \ = False, push_freq = 10):\n        gr.sync_block.__init__(\n            self,\n\
      \            name=\"Byte Vector Source\",\n            in_sig=None,\n      \
      \      out_sig=[np.uint8]\n        )\n        self.sample_rate = sample_rate\n\
      \        self.seq = seq\n        self._seq_manual = seq & 0xFF\n        self.mode\
      \ = mode\n        self.cmd_option = cmd_option\n        self.pad_mode = pad_mode\n\
      \        self.chunk_bits = 1\n        self.ptr = 0\n        self.chunk_bits\
      \ = chunk_bits\n        self.rand_pad = rand_pad\n        self.push_period =\
      \ 1/push_freq\n        self.lock = threading.Lock()\n        if self.mode ==\
      \ \"client\":\n            self.vector = np.zeros(8, dtype=np.uint8)  # \u521D\
      \u59CB\u7A7A\u5E27\n            self.v_len = len(self.vector)\n            self._start_zmq_thread()\n\
      \            print(\"Byte Source in CLIENT mode, waiting for ZMQ payload...\"\
      )\n        else:\n            if cmd_option not in CMD_OPTIONS:\n          \
      \      raise ValueError(f\"cmd_option must be one of {list(CMD_OPTIONS.keys())}\"\
      )\n            self.vector = self._build_frame(payload)\n            if self.rand_pad:\n\
      \                pad_num = self.push_period * sample_rate - len(self.vector)\n\
      \                np.append(self.vector,list(random.randbytes(pad_num)))\n  \
      \          self.vector = self._bytes_to_chunks(self.vector, self.chunk_bits)\n\
      \            self.v_len = len(self.vector)\n            print(f\"Byte Source\
      \ Initialized (mode=chunks(2b), len={self.v_len}, cmd=0x{self._cmd_id:04X})\"\
      )\n\n    \n\n    def _start_zmq_thread(self):\n        def zmq_worker():\n \
      \           ctx = zmq.Context()\n            sock = ctx.socket(zmq.PULL)\n \
      \           sock.bind(\"tcp://*:5555\")\n            while True:\n         \
      \       try:\n                    msg = sock.recv_json()\n                 \
      \   # \u671F\u671Bmsg\u4E3Adict\uFF0C\u5305\u542Bcmd_option\u548Cpayload\u5B57\
      \u6BB5\n                    cmd_option = msg.get(\"cmd_option\", \"enemy_pos\"\
      )\n                    payload = {k: v for k, v in msg.items() if k != \"cmd_option\"\
      }\n                    if cmd_option not in CMD_OPTIONS:\n                 \
      \       continue\n                    frame = self._build_frame(payload, cmd_option=cmd_option)\n\
      \                    chunks = self._bytes_to_chunks(frame, self.chunk_bits)\n\
      \                    with self.lock:\n                        self.vector =\
      \ chunks\n                        self.v_len = len(chunks)\n               \
      \         self.ptr = 0\n                    print(f\"[ZMQ] Received payload\
      \ for {cmd_option}, frame updated.\")\n                except Exception as e:\n\
      \                    print(f\"[ZMQ] Error: {e}\")\n        t = threading.Thread(target=zmq_worker,\
      \ daemon=True)\n        t.start()\n\n    def _build_frame(self, payload, cmd_option=None):\n\
      \        if self._seq_manual == -1:\n            seq = random.randint(0,65535)\n\
      \        if cmd_option is None:\n            cmd_option = self.cmd_option\n\
      \        self._cmd_id, data_len_expect = CMD_OPTIONS[cmd_option]\n        #\
      \ \u7F16\u7801payload\n        if cmd_option == \"jamming\":\n            if\
      \ payload is None:\n                payload = \"RM2026\"\n            if isinstance(payload,\
      \ str):\n                data_bytes = [ord(c) for c in payload.strip().strip(\"\
      '\\\"\")]\n                if len(data_bytes) < data_len_expect:\n         \
      \           data_bytes += [0x00] * (data_len_expect - len(data_bytes))\n   \
      \             if len(data_bytes) > data_len_expect:\n                    data_bytes\
      \ = data_bytes[:data_len_expect]\n            else:\n                data_bytes\
      \ = _encode_payload(cmd_option, payload, self.pad_mode)\n        else:\n   \
      \         if payload is None:\n                payload = {}\n            data_bytes\
      \ = _encode_payload(cmd_option, payload, self.pad_mode)\n            if len(data_bytes)\
      \ < data_len_expect:\n                data_bytes += [0x00] * (data_len_expect\
      \ - len(data_bytes))\n            if len(data_bytes) > data_len_expect:\n  \
      \              data_bytes = data_bytes[:data_len_expect]\n        data_len =\
      \ len(data_bytes)\n        sof = 0xA5\n        len_l = data_len & 0xFF\n   \
      \     len_h = (data_len >> 8) & 0xFF\n        seq_derived = self._seq_manual\
      \ & 0xFF\n        header_wo_crc8 = [sof, len_l, len_h, seq_derived]\n      \
      \  crc8 = _crc8(header_wo_crc8)\n        frame_header = header_wo_crc8 + [crc8]\n\
      \        cmd_l = self._cmd_id & 0xFF\n        cmd_h = (self._cmd_id >> 8) &\
      \ 0xFF\n        body = [cmd_l, cmd_h] + data_bytes\n        crc16 = _crc16(frame_header\
      \ + body)\n        crc16_l = crc16 & 0xFF\n        crc16_h = (crc16 >> 8) &\
      \ 0xFF\n        frame = frame_header + body + [crc16_l, crc16_h]\n        return\
      \ np.array(frame, dtype=np.uint8)\n\n    def _bytes_to_chunks(self, byte_arr,\
      \ chunk_bits=2):\n        \"\"\"\n        Convert an array of bytes (np.uint8)\
      \ into an array of chunk values.\n        - Interpret each byte MSB-first.\n\
      \        - Group bits into chunks of `chunk_bits` (e.g. 2), producing values\
      \ 0..(2^chunk_bits-1).\n        - If total bits is not divisible by chunk_bits,\
      \ pad with zeros at the end.\n        Returns np.array(dtype=np.uint8) of chunk\
      \ values.\n        \"\"\"\n        if chunk_bits <= 0:\n            raise ValueError(\"\
      chunk_bits must >=1\")\n        arr = np.asarray(byte_arr, dtype=np.uint8)\n\
      \        # Extract bits MSB-first per byte\n        try:\n            bits =\
      \ np.unpackbits(arr, bitorder='big')\n        except TypeError:\n          \
      \  # Older numpy versions may not support bitorder kw; fallback\n          \
      \  bits = np.empty(len(arr) * 8, dtype=np.uint8)\n            for i, b in enumerate(arr):\n\
      \                for j in range(8):\n                    bits[i * 8 + j] = (int(b)\
      \ >> (7 - j)) & 1\n\n        total_bits = bits.size\n        # Pad to multiple\
      \ of chunk_bits\n        rem = total_bits % chunk_bits\n        if rem != 0:\n\
      \            pad = chunk_bits - rem\n            bits = np.concatenate((bits,\
      \ np.zeros(pad, dtype=np.uint8)))\n\n        # Reshape and convert to integer\
      \ values\n        bits2 = bits.reshape(-1, chunk_bits)\n        # weights: most-significant\
      \ bit has highest weight\n        weights = (1 << np.arange(chunk_bits - 1,\
      \ -1, -1)).astype(np.uint8)\n        vals = bits2.dot(weights).astype(np.uint8)\n\
      \        return vals\n\n    def work(self, input_items, output_items):\n   \
      \     out = output_items[0]\n        n = len(out)\n        with self.lock:\n\
      \            for i in range(n):\n                out[i] = self.vector[self.ptr]\n\
      \                self.ptr = (self.ptr + 1) % self.v_len\n        return n\n"
    affinity: ''
    alias: ''
    chunk_bits: '1'
    cmd_option: '"jamming"'
    comment: ''
    maxoutbuf: '0'
    minoutbuf: '0'
    mode: '''local'''
    pad_mode: '''pad'''
    payload: None
    push_freq: '10'
    rand_pad: 'True'
    sample_rate: 1e6
    seq: '3'
  states:
    _io_cache: "('Byte Vector Source', 'byte_vector_source', [('sample_rate', '1000000.0'),\
      \ ('payload', 'None'), ('cmd_option', \"'enemy_pos'\"), ('seq', '-1'), ('chunk_bits',\
      \ '1'), ('pad_mode', \"'pad'\"), ('mode', \"'local'\"), ('rand_pad', 'False'),\
      \ ('push_freq', '10')], [], [('0', 'byte', 1)], '\\n\u5C06\u8F93\u5165\u6570\
      \u636E\uFF08dict\u6216\u5B57\u7B26\u4E32\uFF09\u5C01\u88C5\u4E3A RoboMaster\
      \ \u534F\u8BAE\u5E27\u5E76\u5FAA\u73AF\u8F93\u51FA\u3002\\n\u652F\u6301\u672C\
      \u5730\u53C2\u6570\uFF08mode=local\uFF09\u6216\u5916\u90E8ZMQ\u5BA2\u6237\u7AEF\
      \u53C2\u6570\uFF08mode=client\uFF09\u3002\\n\u5E27\u7ED3\u6784: [SOF|len(2)|seq|CRC8][cmd_id(2)][data][CRC16(2)]\\\
      n', ['chunk_bits', 'cmd_option', 'mode', 'pad_mode', 'rand_pad', 'sample_rate',\
      \ 'seq'])"
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [24, 420.0]
    rotation: 0
    state: enabled
- name: epy_block_0_0
  id: epy_block
  parameters:
    _source_code: "\"\"\"\nEmbedded Python Block: ZMQ PULL + JSON \u89E3\u6790\n-\
      \ \u5185\u90E8\u521B\u5EFA ZMQ PULL socket\uFF0C\u63A5\u6536 JSON \u6D88\u606F\
      \n- \u89E3\u6790\u51FA center_freq_mhz, bandwidth_mhz, access_code\n- \u8F93\
      \u51FA\u4E09\u4E2A message \u7AEF\u53E3: freq (int), bw (int), access_code (string)\n\
      \"\"\"\n\nimport json\nimport threading\nimport zmq\nfrom gnuradio import gr\n\
      import pmt\nimport time\n\nclass blk(gr.sync_block):\n    def __init__(self,\
      \ endpoint=\"tcp://127.0.0.1:5555\", bind=False,rx_type = \"inter\"):\n    \
      \    gr.sync_block.__init__(\n            self,\n            name='ZMQ JSON\
      \ Pull Parser (int freq/bw + access_code)',\n            in_sig=None,\n    \
      \        out_sig=None\n        )\n        if rx_type not in [\"inter\", \"boardcast\"\
      ]:\n            raise Exception(\"rx_type must be 'inter' or 'boardcast'\")\n\
      \        else:\n            self.rx_type = rx_type\n        self.endpoint =\
      \ endpoint\n        self.bind = bind\n        self.running = False\n       \
      \ self.thread = None\n\n        # \u6CE8\u518C\u4E09\u4E2A\u8F93\u51FA\u7AEF\
      \u53E3\n        self.message_port_register_out(pmt.intern('freq'))        #\
      \ int\n        self.message_port_register_out(pmt.intern('bw'))          # int\n\
      \        self.message_port_register_out(pmt.intern('access_code')) # string\n\
      \n        # \u542F\u52A8\u63A5\u6536\u7EBF\u7A0B\n        self.start_receiver()\n\
      \n    def start_receiver(self):\n        self.running = True\n        self.thread\
      \ = threading.Thread(target=self.receiver_loop)\n        self.thread.daemon\
      \ = True\n        self.thread.start()\n\n    def receiver_loop(self):\n    \
      \    context = zmq.Context()\n        socket = context.socket(zmq.SUB)\n\n \
      \       socket.setsockopt(zmq.CONFLATE, 1)      # \u53EA\u4FDD\u7559\u6700\u65B0\
      \u547D\u4EE4\n        socket.setsockopt(zmq.SUBSCRIBE, b\"\")    # \u8BA2\u9605\
      \u6240\u6709\u4E3B\u9898\n        socket.setsockopt(zmq.RCVTIMEO, 10000)   #\
      \ 30\u79D2\u63A5\u6536\u8D85\u65F6\uFF0C\u9632\u6B62\u65E0\u9650\u671F\u963B\
      \u585E\n        if self.bind:\n            socket.bind(self.endpoint)\n    \
      \        print(f\"[ZMQ] Binding PULL to {self.endpoint}\")\n        else:\n\
      \            socket.connect(self.endpoint)\n            print(f\"[ZMQ] Connecting\
      \ PULL to {self.endpoint}\")\n\n        while self.running:\n            try:\n\
      \                msg_bytes = socket.recv()\n                self.process_message(msg_bytes)\n\
      \            except zmq.Again:\n                print(\"[ZMQ] Receive timeout,\
      \ no message received\")\n                continue    \n            except zmq.ZMQError\
      \ as e:\n                print(f\"[ZMQ] Error: {e}\")\n                break\n\
      \            except Exception as e:\n                print(f\"[ZMQ] Unexpected\
      \ error: {e}\")\n                break\n\n        socket.close()\n        context.term()\n\
      \        print(\"[ZMQ] Receiver thread stopped\")\n        if self.delay > 0:\n\
      \            time.sleep(self.delay)\n\n    def process_message(self, msg_bytes):\n\
      \        try:\n            s = msg_bytes.decode('utf-8')\n            data =\
      \ json.loads(s)\n        except Exception as e:\n            print(f\"[Parser]\
      \ JSON decode error: {e}\")\n            return\n        data = data[self.rx_type]\n\
      \        # \u63D0\u53D6\u5B57\u6BB5\uFF0C\u7F3A\u5931\u65F6\u4F7F\u7528\u9ED8\
      \u8BA4\u503C\u5E76\u62A5\u8B66\n        freq_val = data.get('center_freq')\n\
      \        bw_val   = data.get('bandwidth')\n        code_val = data.get('access_code')\n\
      \n        if freq_val is None or bw_val is None or code_val is None:\n     \
      \       missing = []\n            if freq_val is None: missing.append('center_freq')\n\
      \            if bw_val is None: missing.append('bandwidth')\n            if\
      \ code_val is None: missing.append('access_code')\n            print(f\"[Parser]\
      \ Missing keys: {missing} in {data.keys()}\")\n            return\n\n      \
      \  # \u8F6C\u6362\u4E3A int (\u622A\u65AD\u5C0F\u6570)\n        freq_int = int(freq_val)\n\
      \        bw_int   = int(bw_val)\n\n        # \u53D1\u9001\u6D88\u606F\n    \
      \    self.message_port_pub(pmt.intern('freq'), \n                          \
      \    pmt.cons(pmt.intern('freq'), pmt.from_long(freq_int)))\n        self.message_port_pub(pmt.intern('bw'),\
      \ \n                              pmt.cons(pmt.intern('bw'), pmt.from_long(bw_int)))\n\
      \        self.message_port_pub(pmt.intern('access_code'), \n               \
      \               pmt.cons(pmt.intern('access_code'), pmt.intern(code_val)))\n\
      \n        print(f\"[Parser] Sent: freq={freq_int/1e6} MHz, bw={bw_int/1e6} MHz,\
      \ access_code={code_val}\")\n\n    def stop(self):\n        self.running = False\n\
      \        if self.thread and self.thread.is_alive():\n            self.thread.join(timeout=2.0)\n\
      \n    def __del__(self):\n        self.stop()"
    affinity: ''
    alias: ''
    bind: 'False'
    comment: ''
    endpoint: '''tcp://127.0.0.1:5555'''
    maxoutbuf: '0'
    minoutbuf: '0'
    rx_type: '''inter'''
  states:
    _io_cache: ('ZMQ JSON Pull Parser (int freq/bw + access_code)', 'blk', [('endpoint',
      "'tcp://127.0.0.1:5555'"), ('bind', 'False'), ('rx_type', "'inter'")], [], [('access_code',
      'message', 1), ('bw', 'message', 1), ('freq', 'message', 1)], '', ['bind', 'endpoint',
      'rx_type'])
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [392, 8.0]
    rotation: 0
    state: enabled
- name: iio_pluto_sink_0
  id: iio_pluto_sink
  parameters:
    affinity: ''
    alias: ''
    attenuation1: '10.0'
    bandwidth: int(bw)
    buffer_size: '32768'
    comment: ''
    cyclic: 'False'
    filter: ''
    filter_source: '''Auto'''
    fpass: '0'
    frequency: int(cent_freq)
    fstop: '0'
    len_tag_key: ''
    samplerate: int(samp_rate)
    type: fc32
    uri: ''
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [888, 412.0]
    rotation: 0
    state: enabled

connections:
- [blocks_tagged_stream_mux_0, '0', digital_gfsk_mod_0, '0']
- [digital_gfsk_mod_0, '0', iio_pluto_sink_0, '0']
- [digital_protocol_formatter_bb_0, '0', blocks_tagged_stream_mux_0, '0']
- [epy_block_0, '0', blocks_tagged_stream_mux_0, '1']
- [epy_block_0, '0', digital_protocol_formatter_bb_0, '0']
- [epy_block_0_0, access_code, blocks_msgpair_to_var_1_0, inpair]
- [epy_block_0_0, bw, blocks_msgpair_to_var_1, inpair]
- [epy_block_0_0, freq, blocks_msgpair_to_var_0, inpair]

metadata:
  file_format: 1
  grc_version: 3.10.12.0
